import React, { Component } from 'react';

class CreateButton extends Component{
    render(){
        return(
            <button type="button" name="createSquare" value="createSquare">四角を生成する</button>
        );
    }
}

export default CreateButton;